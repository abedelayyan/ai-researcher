version: 2
updates:
  # Actions get pinned by major tag, so this catches the ones that go stale.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: monthly
